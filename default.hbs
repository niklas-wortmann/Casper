<!DOCTYPE html>
<html lang="{{@site.locale}}" class="{{#match @custom.color_scheme "Dark"}}dark-mode{{else match @custom.color_scheme "Auto"}}auto-color{{/match}}{{#is "home"}} no-infinite-scroll{{/is}}">
<head>

    {{!-- Basic meta - advanced meta is output with {ghost_head} below --}}
    <title>{{meta_title}}</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    {{!-- Preload scripts --}}
    <link rel="preload" as="style" href="{{asset "built/screen.css"}}" />
    <link rel="preload" as="script" href="{{asset "built/casper.js"}}" />

    {{!-- Theme assets - use the {asset} helper to reference styles & scripts,
    this will take care of caching and cache-busting automatically --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />
    <style>
        .weekly-brew-logo {
            display: none;
            height: 40px;
            width: auto;
        }

        .weekly-brew-logo--dark {
            display: inline-block;
        }

        .weekly-brew-tag-logo {
            display: flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 18px;
        }

        .weekly-brew-tag-logo img {
            height: 64px;
            width: auto;
        }

        .weekly-brew-post-logo {
            display: flex;
            align-items: center;
            margin: 16px 0 8px;
        }

        .weekly-brew-post-logo img {
            height: 56px;
            width: auto;
        }

        .gh-head-logo .site-logo--avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }

        .hero-brand .site-logo--avatar {
            width: 88px;
            height: 88px;
        }

        html.dark-mode .weekly-brew-logo--dark {
            display: none;
        }

        html.dark-mode .weekly-brew-logo--light {
            display: inline-block;
        }

        @media (prefers-color-scheme: dark) {
            html.auto-color .weekly-brew-logo--dark {
                display: none;
            }

            html.auto-color .weekly-brew-logo--light {
                display: inline-block;
            }
        }

        .gh-head-logo .weekly-brew-logo--dark {
            display: none;
        }

        .gh-head-logo .weekly-brew-logo--light {
            display: inline-block;
        }
    </style>

    {{!-- This tag outputs all your advanced SEO meta, structured data, and other important settings,
    it should always be the last tag before the closing head tag --}}
    {{ghost_head}}

</head>
<body class="{{body_class}} is-head-{{#match @custom.navigation_layout "Logo on cover"}}left-logo{{else match @custom.navigation_layout "Logo in the middle"}}middle-logo{{else}}stacked{{/match}}{{#match @custom.title_font "=" "Elegant serif"}} has-serif-title{{/match}}{{#match @custom.body_font "=" "Modern sans-serif"}} has-sans-body{{/match}}{{#if @custom.show_publication_cover}} has-cover{{/if}}">
<div class="viewport">

    <header id="gh-head" class="gh-head outer{{#match @custom.header_style "Hidden"}} is-header-hidden{{/match}}">
        <div class="gh-head-inner inner">
            <div class="gh-head-brand">
                <a class="gh-head-logo" href="{{@site.url}}">
                    {{#is "tag"}}
                        {{#tag}}
                            {{#match slug "newsletter"}}
                                <img class="weekly-brew-logo weekly-brew-logo--dark" src="{{asset "images/weekly-devs-brew-logo-dark.svg"}}" alt="The Weekly Dev's Brew">
                                <img class="weekly-brew-logo weekly-brew-logo--light" src="{{asset "images/weekly-devs-brew-logo-light.svg"}}" alt="The Weekly Dev's Brew">
                            {{else match slug "podcast"}}
                                <img class="weekly-brew-logo weekly-brew-logo--dark" src="{{asset "images/weekly-devs-brew-logo-dark.svg"}}" alt="The Weekly Dev's Brew">
                                <img class="weekly-brew-logo weekly-brew-logo--light" src="{{asset "images/weekly-devs-brew-logo-light.svg"}}" alt="The Weekly Dev's Brew">
                            {{else}}
                                {{#match @site.title "=" "wordman.dev"}}
                                    <img class="site-logo--avatar" src="{{asset "images/wordman-avatar.svg"}}" alt="Jan-Niklas Wortmann">
                                {{else}}
                                    {{#if @site.logo}}
                                        <img src="{{@site.logo}}" alt="{{@site.title}}">
                                    {{else}}
                                        {{@site.title}}
                                    {{/if}}
                                {{/match}}
                            {{/match}}
                        {{/tag}}
                    {{else}}
                        {{#is "post"}}
                            {{#has tag="newsletter,podcast"}}
                                <img class="weekly-brew-logo weekly-brew-logo--dark" src="{{asset "images/weekly-devs-brew-logo-dark.svg"}}" alt="The Weekly Dev's Brew">
                                <img class="weekly-brew-logo weekly-brew-logo--light" src="{{asset "images/weekly-devs-brew-logo-light.svg"}}" alt="The Weekly Dev's Brew">
                            {{else}}
                                {{#match @site.title "=" "wordman.dev"}}
                                    <img class="site-logo--avatar" src="{{asset "images/wordman-avatar.svg"}}" alt="Jan-Niklas Wortmann">
                                {{else}}
                                    {{#if @site.logo}}
                                        <img src="{{@site.logo}}" alt="{{@site.title}}">
                                    {{else}}
                                        {{@site.title}}
                                    {{/if}}
                                {{/match}}
                            {{/has}}
                        {{else}}
                            {{#match @site.title "=" "wordman.dev"}}
                                <img class="site-logo--avatar" src="{{asset "images/wordman-avatar.svg"}}" alt="Jan-Niklas Wortmann">
                            {{else}}
                                {{#if @site.logo}}
                                    <img src="{{@site.logo}}" alt="{{@site.title}}">
                                {{else}}
                                    {{@site.title}}
                                {{/if}}
                            {{/match}}
                        {{/is}}
                    {{/is}}
                </a>
                <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                <button class="gh-burger" aria-label="Main Menu"></button>
            </div>

            <nav class="gh-head-menu">
                {{navigation}}
                {{#unless @site.members_enabled}}
                    {{#match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{/unless}}
            </nav>

            <div class="gh-head-actions">
                {{#unless @site.members_enabled}}
                    {{^match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{else}}
                    <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    <div class="gh-head-members">
                        {{#unless @member}}
                            {{#unless @site.members_invite_only}}
                                <a class="gh-head-button" href="#/portal/signup" data-portal="signup">Subscribe</a>
                            {{/unless}}
                        {{else}}
                            <a class="gh-head-button" href="#/portal/account" data-portal="account">Account</a>
                        {{/unless}}
                    </div>
                {{/unless}}
            </div>
        </div>
    </header>

    <div class="site-content">
        {{!-- All other templates get inserted here, index.hbs, post.hbs, etc --}}
        {{{body}}}
    </div>

    {{!-- The global footer at the very bottom of the screen --}}
    <footer class="site-footer outer">
        <div class="inner">
            <section class="copyright"><a href="{{@site.url}}">{{@site.title}}</a> &copy; {{date format="YYYY"}}</section>
            <nav class="site-footer-nav">
                {{navigation type="secondary"}}
                <div class="site-footer-extras">
                    <div class="site-footer-block">
                        <div class="site-footer-label">Connect</div>
                        <div class="gh-social">
                            <a class="gh-social-link" href="https://bsky.app/profile/wordman.dev" target="_blank" rel="noopener" aria-label="Bluesky">{{> "icons/bluesky"}}</a>
                            <a class="gh-social-link" href="https://twitter.com/niklas_wortmann" target="_blank" rel="noopener" aria-label="X (Twitter)">{{> "icons/twitter"}}</a>
                            <a class="gh-social-link" href="https://github.com/niklas_wortmann" target="_blank" rel="noopener" aria-label="GitHub">{{> "icons/github"}}</a>
                            <a class="gh-social-link" href="https://www.linkedin.com/in/jan-niklas-wortmann/" target="_blank" rel="noopener" aria-label="LinkedIn">{{> "icons/linkedin"}}</a>
                        </div>
                    </div>
                    <div class="site-footer-block">
                        <div class="site-footer-label">Podcast</div>
                        <div class="site-footer-links">
                            <a href="https://open.spotify.com/show/6uG7XUDtY6QqkjG5RmgvPZ" target="_blank" rel="noopener">Spotify</a>
                            <a href="https://podcasts.apple.com/us/podcast/the-weekly-devs-brew/id1818009834" target="_blank" rel="noopener">Apple Podcasts</a>
                            <a href="https://www.youtube.com/channel/UC7aHZVHaVcHpu3m8vWstVxg" target="_blank" rel="noopener">YouTube</a>
                            <a href="https://feeds.buzzsprout.com/2470901.rss" target="_blank" rel="noopener">RSS</a>
                        </div>
                    </div>
                </div>
            </nav>
            <div class="gh-powered-by"><a href="https://ghost.org/" target="_blank" rel="noopener">Powered by Ghost</a></div>
        </div>
    </footer>

</div>
{{!-- /.viewport --}}

{{#is "post, page"}}
    {{> "lightbox"}}
{{/is}}

{{!-- Scripts - handle member signups, responsive videos, infinite scroll, floating headers, and galleries --}}
<script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous">
</script>
<script src="{{asset "built/casper.js"}}"></script>
<script>
$(document).ready(function () {
    // Mobile Menu Trigger
    $('.gh-burger').click(function () {
        $('body').toggleClass('gh-head-open');
    });
    // FitVids - Makes video embeds responsive
    $(".gh-content").fitVids();
});
</script>

{{!-- Ghost outputs required functional scripts with this tag - it should always be the last thing before the closing body tag --}}
{{ghost_foot}}

</body>
</html>
